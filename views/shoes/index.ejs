<%- include('../partials/header') %>

<table>
  <thead>
    <tr>
      <th>Brand</th>
      <th>Size</th>
      <th>Color</th>
    </tr>
  </thead>
<tbody>
<% shoes.forEach(function(s) { %>
  <tr>
    <td><%= s.brand %></td>
    <td><%= s.size %></td>
    <td><%= s.color %></td>
  </tr>
<% }) %>
</tbody>  
</table>

<%- include('../partials/footer') %>